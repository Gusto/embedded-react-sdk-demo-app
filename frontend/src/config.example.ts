// Copy this file to config.ts and fill in your IDs. config.ts is gitignored
// (like .env) because it holds the ids for your own demo company. See the README.

// Your partner-managed company's id.
export const COMPANY_ID = "";

// The employee whose self-onboarding flow the self-onboarding demo runs. Leave
// undefined until you have an employee: run the Employee onboarding demo to
// create one, then copy its id out of the browser URL
// (/employee-onboarding/<employeeId>/...). The self-onboarding demo redirects
// home until this is set.
export const EMPLOYEE_ID: string | undefined = undefined;
